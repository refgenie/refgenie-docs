---
title: "BiocRefgetStore: Benchmark"
---

:::note[Placeholder page]
This page is generated from the **BiocRefgetStore** R package vignettes by the
docs build pipeline (`scripts/render_r_vignettes.R`, run via bulker/Docker).
That step needs the BiocRefgetStore package source (`BIOCREFGETSTORE_SRC`),
which isn't available in this local checkout, so this is a placeholder to let the
site build locally. The real content is rendered in CI / a full build.
:::
